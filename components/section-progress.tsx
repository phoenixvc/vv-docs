"use client"

interface SectionProgressProps {
  activeSection: string
}

export function SectionProgress({ activeSection }: SectionProgressProps) {
  // This is a placeholder component that could show which section the user is currently viewing
  // For example, it could highlight the current section in a side navigation or show a progress indicator
  return null
}
